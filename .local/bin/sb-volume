#!/bin/sh

_bsd() {
	sndioctl -n output.level | sed 's/0\.//' | sed 's/.$/%/' | sed 's/\.//'
}

_linux() {
	pactl get-sink-volume @DEFAULT_SINK@ | awk '{print $5}'
}

case $(uname) in
	Linux) _linux ;;
	*BSD) _bsd ;;
esac
